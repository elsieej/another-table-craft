version: 2
updates:
  # Once the demo/ workspace exists (#22), add a second entry for it here —
  # Dependabot errors on a directory that doesn't exist yet rather than skipping it.
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'weekly'
